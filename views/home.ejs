<%- include('components/header') %>

<div class="w-full h-screen bg-[#ABABAB] flex flex-col">
  <!-- Header -->
  <header class="h-[8vh] bg-[#D7D7D7] flex items-center rounded-b-3xl justify-between px-6 shadow">
    <!-- Left: Logo + Title -->
    <div class="flex items-center">
      <a href="#"><img src="/images/logo.jpg" class="w-[6vh] h-[6vh] rounded-full" alt="logo"></a>
      <h1 class="text-2xl font-bold ml-6">ChatRoom</h1>
    </div>

    <!-- Right: Dropdown -->
    <div class="relative inline-block" id="dropdownWrapper">
      <button id="dropdownButton" aria-haspopup="true" aria-expanded="false"
        class="flex items-center hover:cursor-pointer">
        <div class="w-[6vh] h-[6vh] rounded-full overflow-hidden">
          <img src="<%= currentUser.profilePicture || '/images/default.jpg' %>" alt="profile"
            class="w-full h-full object-cover">
        </div>
        <svg viewBox="0 0 20 20" fill="currentColor" class="w-5 h-5 text-gray-500 ml-2">
          <path
            d="M5.22 8.22a.75.75 0 0 1 1.06 0L10 11.94l3.72-3.72a.75.75 0 1 1 1.06 1.06l-4.25 4.25a.75.75 0 0 1-1.06 0L5.22 9.28a.75.75 0 0 1 0-1.06Z" />
        </svg>
      </button>
      <!-- Dropdown Menu -->
      <div id="dropdownMenu"
        class="hidden absolute right-0 mt-2 w-56 bg-white rounded-md shadow-lg z-50 transition-all duration-200 transform opacity-0 scale-95">
        <a href="/home" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">Home</a>
        <a href="/profile" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">Profile</a>
        <a href="/" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">Add Account</a>
        <a href="/logout" class="block px-4 py-2 text-red-600 hover:bg-gray-100">Logout</a>
      </div>
    </div>
  </header>

  <!-- Main Layout -->
  <main class="flex flex-row gap-2 mt-[1vh] w-full h-[91vh] px-2">
    <!-- Left: Profile Section -->
    <div id="Users"
      class="bg-[#D7D7D7] w-[22%] rounded-3xl p-4 h-full shadow flex flex-col items-center text-center">
      <!-- Profile Picture -->
      
    </div>

    <!-- Center: Chat Area -->
    <div id="chat-area"
      class="bg-[#D7D7D7] w-[58%] h-full rounded-3xl shadow flex flex-col">
      <!-- Messages -->
      <div class="flex-1 p-4 overflow-y-auto space-y-3">
        <p class="text-gray-500 text-center"></p>
        <!-- Example messages -->
        <div class="flex justify-start">
          <div class="bg-white px-4 py-2 rounded-2xl shadow">Hello 👋</div>
        </div>
        <div class="flex justify-end">
          <div class="bg-green-200 px-4 py-2 rounded-2xl shadow">Hi, how are you?</div>
        </div>
      </div>
      <!-- Input -->
      <div class="p-3 bg-[#ECECEC] rounded-b-3xl">
        <form class="flex gap-2">
          <input type="text" placeholder="Type a message..."
            class="flex-1 rounded-full px-4 py-2 outline-none focus:ring-2 focus:ring-green-500">
          <button type="submit"
            class="px-4 py-2 bg-green-500 text-white rounded-full hover:bg-green-600 transition">Send</button>
        </form>
      </div>
    </div>

    <!-- Right: Search + User List -->
    <div id="users-list" class="bg-[#D7D7D7] rounded-3xl w-[20%] h-full shadow flex flex-col overflow-hidden">
      <!-- Search Bar -->
      <div class="p-3 bg-[#ECECEC] rounded-t-3xl sticky top-0 z-10">
        <form method="GET" action="/home" class="mb-2">
          <input type="text" name="search" placeholder="Search users"
            class="w-full h-[5vh] bg-white text-center font-semibold rounded-full outline-none focus:ring-2 focus:ring-green-500">
        </form>
      </div>

      <!-- Scrollable User List -->
      <div class="flex-1 overflow-y-auto p-3 space-y-4">
        <div>
          <h2 class="text-lg font-bold text-center">Requests</h2>
          <!-- Example request -->
          <div class="flex items-center justify-between bg-white px-3 py-2 rounded-2xl shadow">
            <span class="font-medium">John Doe</span>
            <div class="flex gap-2">
              <button class="px-3 py-1 bg-green-500 text-white rounded-full text-sm">Accept</button>
              <button class="px-3 py-1 bg-red-500 text-white rounded-full text-sm">Decline</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
</div>

<!-- Dropdown Script -->
<script>
  const button = document.getElementById('dropdownButton');
  const menu = document.getElementById('dropdownMenu');

  button.addEventListener('click', () => {
    const expanded = button.getAttribute("aria-expanded") === "true" || false;
    button.setAttribute("aria-expanded", !expanded);

    menu.classList.toggle('hidden');
    menu.classList.toggle('opacity-0');
    menu.classList.toggle('scale-95');
  });

  window.addEventListener('click', (e) => {
    if (!document.getElementById('dropdownWrapper').contains(e.target)) {
      menu.classList.add('hidden', 'opacity-0', 'scale-95');
      button.setAttribute("aria-expanded", "false");
    }
  });
</script>

<%- include('components/footer') %>
